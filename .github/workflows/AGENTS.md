# GitHub Workflows Context

**Purpose:**
This directory contains the YAML files that define GitHub Actions workflows.

**Key Files:**
- `scraper.yml`: Configures the scheduled cron job to run the Puppeteer scraper (`run-scraper.js`) automatically.

**How AI should use this folder:**
Modify files here to change CI/CD pipelines, scheduled jobs, or automated testing behavior in GitHub Actions.
