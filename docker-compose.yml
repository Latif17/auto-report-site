version: '3.8'

services:
  scraper:
    build: .
    container_name: auto-report-scraper
    restart: always
    environment:
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_KEY=${SUPABASE_KEY}
